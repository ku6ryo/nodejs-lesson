/**
 * キーが "apple" で値が 2, キーが "banana" で値が 88 のオブジェクトを作成してください
 * Create an object with a key is "apple" and the value is 2, and a key is "banana" and the value is 88.
 */

function main () {
  const obj = // ここに作成して代入 / create and insert into this variable.
  console.log(obj["apple"]) // 2 が表示されるべき / 2 shold be printed.
  console.log(obj["banana"]) // 88 が表示されるべき / 88 should be printed.
}
main()
