/**
 * Get all keys of an object (obj), make an array of them and sort in ascending order.
 * オブジェクトのすべてのキーを取得して配列にして、昇順に並び替えてください。
 */

function main () {
  const obj = {
    "banana": 2,
    "apple": 1,
    "dog": 4,
    "cookie": 3,
  }
  const keys = // ここに代入 / insert the result array
  console.log(keys[0]) // "apple" が表示されるべき / "apple" should be printed.
  console.log(keys[1]) // "banana" が表示されるべき / "banana" should be printed.
}
main()
