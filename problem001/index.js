/**
 * add 足し算　/ substruct 引き算の関数を作成してください
 */

function add (a, b) {
  // a + b の答えを返すようにしてください
}

function substract (a, b) {
  // a - b の答えを返すようにしてください
}

function main () {
  console.log(add(1, 2)) // 3 が表示されるように
  console.log(add(2, 8)) // 10 が表示されるように
  console.log(substract(10, 3)) // 7 が表示されるように
  console.log(substract(4, 6)) // -2 が表示されるように
}
main()
